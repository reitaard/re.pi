This RePi release introduces Git-derived downstream versioning, a RePi-only self-update channel, Open Provider and OpenAI OAuth model discovery, and the Kioku durable-memory foundation.

The updater never installs the upstream Pi package and preserves the existing `.pi` configuration and session locations.
