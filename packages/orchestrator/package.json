{
	"name": "@reitaard/repi-orchestrator",
	"version": "0.81.4",
	"description": "experimental orchestrator package for pi",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist",
		"README.md",
		"CHANGELOG.md"
	],
	"scripts": {
		"clean": "shx rm -rf dist",
		"dev": "tsgo -p tsconfig.build.json --watch --preserveWatchOutput",
		"build": "tsgo -p tsconfig.build.json && shx chmod +x dist/cli.js",
		"test": "node --experimental-strip-types --test test/target-routing.test.mjs",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"keywords": [
		"pi",
		"orchestrator"
	],
	"author": "Earendil Works",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/reitaard/re.pi.git",
		"directory": "packages/orchestrator"
	},
	"engines": {
		"node": ">=22.19.0"
	},
	"dependencies": {
		"@reitaard/repi-coding-agent": "^0.81.4"
	},
	"devDependencies": {
		"shx": "0.4.0"
	}
}
